## 主空间

=agent-space；勿改兄弟项目

## 资料

`research/`：中间调研；可保留，是否清理由用户决定

`docs/`：调研后的方向概要

`.tmp/`：临时文件；用户不必看，用完可删，agent 可复用

## Git

commit 后 push

## Setup

布局：单仓（single-context）
Issue tracker：Local markdown
Triage labels：默认

运行 /my-setup-matt-pocock-skills
